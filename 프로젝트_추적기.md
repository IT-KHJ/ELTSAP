# ELTSAP 프로젝트 추적기

> 현재 진행 중인 작업만 기록합니다. 완료된 이전 단계(1~6단계, 2026-03-04~04-01)는 `프로젝트_추적기_아카이브.md` 참조.
> 세션이 바뀔 때마다 `CURRENT.md`를 먼저 읽고, 상세 내용은 이 파일에서 확인.

---

## 진행 규칙

- 완료 항목: `[x]`
- 미완료 항목: `[ ]`
- 부분 완료/검토 필요: `[~]`
- 결과물 파일이 있으면 경로를 괄호 안에 명시

---

## 7단계 — 히스토리 백필 스크립트 (2026-08-18~)

### 배경

- `getDateMinSync()` 하한(당월 -2개월 1일) 이전의 과거 SAP 데이터(sales/inamt/saleetc/orders)는 기존 동기화 파이프라인이 커버하지 않음.
- 이 하한을 우회해 지정 기간을 직접 조회 후 upsert하는 전용 스크립트가 필요.

### 진행 사항

- [x] `scripts/backfill-history.ts` 작성 (`entity` sales/inamt/saleetc/orders, 시작일/종료일/청크일수 인자 지원, 청크 단위 조회+upsert, 실패 시 재개 커맨드 안내 출력)
- [x] `package.json`에 `backfill:history` 스크립트 등록
- [ ] 실제 SAP 연결 환경에서 소량 기간으로 동작 검증 (아직 미실행 — git 상태상 미커밋)
- [ ] 검증 후 git 커밋
- [ ] (선택) 대량 백필 실행 시 청크일수 조정 필요 여부 확인 (SAP 쿼리 타임아웃 대비)

### 메모

- `customer`/`itemlist`는 날짜 하한이 없어(전체 동기화로 이미 전체 이력 커버) 이 스크립트 대상이 아님.
- SAP는 SELECT만 수행, 원본 미수정 원칙 동일하게 적용됨(`getSapSqlConfig()` 재사용).

---

## 최근 조사 (코드 변경 없음, 참고용)

### 거래처/품목 전체 동기화 오류 조사 (2026-08-18)

- 증상: "SAP SQL Server 연결 정보 또는 SYNC_CUSTOMER_URL이 설정되지 않았습니다." 오류 발생
- 조사 결과: 로컬 `.env.local`에는 `SAP_SQL_SERVER/DATABASE/USER/PASSWORD` 4종이 모두 정상 설정되어 있어 로컬 실행 시엔 재현되지 않아야 함.
- 가설: Vercel 배포 환경에서 실행했을 가능성. `DEPLOY.md`에 따르면 Vercel은 방화벽 이슈로 SAP 직접 연결이 어려워 `SAP_SQL_*`/`SYNC_*_URL`을 선택 항목으로 두고, 실제 동기화는 localhost 실행 앱 기준으로 스케줄링하도록 설계되어 있음.
- 결론: 코드 버그로 보이지 않음. 코드 수정 없이 조사만 진행함.
- [ ] 사용자가 어느 환경(로컬 vs Vercel 배포 URL)에서 실행했는지 확인 필요 — 미해결

---

## 다음 단계 후보 (미착수, 우선순위 미정)

- [ ] RLS 정책 재검토 (`023`→`024` 롤백 이후 대부분 테이블이 전체 허용 상태로 지속 중 — `프로젝트_내용.md` 참조)
