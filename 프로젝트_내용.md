# ELTSAP 프로젝트 내용

> 이 파일은 세션이 바뀌어도 프로젝트 컨텍스트를 즉시 파악할 수 있도록 유지 관리한다.
> AI는 이 파일을 먼저 읽고 작업을 시작한다. 새로운 결정/변경 사항은 반드시 이 파일에 추가한다.

---

## 서비스 개요

**서비스명:** ELTSAP (ELT 거래처 현황 보고서)
**목적:** SAP(ERP)의 거래처·품목·매출·입금·기타출고 데이터를 Supabase로 동기화하고, 이를 조회 전용으로 사용해 거래처별 매출 현황을 보고서/대시보드 형태로 제공하는 사내 업무용 웹앱
**핵심 사용자:** 사내 영업/재무 담당자 (내부 로그인 필요, `/internal`)
**핵심 원칙:** SAP 원본 데이터는 SELECT만 수행하며 절대 수정하지 않음

핵심 화면:
- 대시보드 (`/internal/dashboard`) — 매출 요약, 인사이트, 파레토 분석, 거래처 랭킹
- 거래처 현황 보고서 (`/internal/report`) — 기간·거래처 선택, 마감 기준, 인쇄 최적화(A4 가로 1페이지)
- 거래처 현황 판매기준 A안 (`/internal/sales-status`) / B안 그룹화 (`/internal/sales-status-b`)
- 동기화 관리 (`/internal/sync`) — 엔티티별 수동 동기화 실행, 마지막 동기화 시각/건수 확인
- 거래처 관리 (`/internal/customers`) — 거래처 정보 조회/수정
- 권한 관리 (`/internal/permissions`, admin 전용) — 사용자별 메뉴 접근 권한 설정

---

## 기술 스택

| 영역 | 기술 |
|------|------|
| 프레임워크 | Next.js 14.2.18 (App Router) |
| 언어 | TypeScript 5.6 |
| UI | React 18.3, Tailwind CSS |
| Backend/DB | Supabase (PostgreSQL + Auth), `@supabase/ssr`로 세션 쿠키 관리 |
| SAP 연동 | `mssql` — SAP SQL Server에 직접 접속, SELECT 전용 |
| 엑셀 출력 | `exceljs` |
| 배포 | Vercel (`vercel.json`, framework: nextjs) |
| 로컬 스케줄러 | Windows 작업 스케줄러 + `tsx` 스크립트 (`scripts/run-daily-sync.ts`, `run-orders-sync.ts`) |

---

## 데이터 모델 현황

### 주요 테이블 (`supabase/migrations/001~024`)

| 테이블 | 역할 |
|--------|------|
| `customer` | 거래처 마스터 (SAP OCRD 동기화). useyn/sido/sigun 등 관리 컬럼 추가됨 |
| `itemlist` | 품목 마스터 (SAP OITM 동기화). brand/itemgb로 분류 |
| `sales` | 매출 라인 (SAP INV1). linestatus/vatsumsy 등 확장 — 보고서/대시보드 핵심 소스 |
| `inamt` | 입금 내역 (SAP ORCT) |
| `saleetc` | 기타출고 라인 (SAP IGE1) |
| `orders` | 판매전표 라인 (SAP DLN1/ODLN) — 판매기준 거래처 현황용 |
| `sync_metadata` | 엔티티별 마지막 동기화 시각/건수 |
| `daily_auto_sync_results` / `hourly_sync_results` | 일괄(매일)/시간별 자동 동기화 실행 이력 |
| `maintenance_info` | 서비스 점검 공지 |
| `user_profiles` | 사용자 표시 이름 |
| `menus` / `user_menu_permissions` | 메뉴 마스터 및 사용자별 접근 권한 |

### RLS/권한 이력 (설계 변경 있었음 — 임의 재변경 금지)

- 초기(001)에는 대부분 테이블이 `USING(true) WITH CHECK(true)` 전체 허용 정책이었음.
- `023_fix_rls_always_true_policies.sql`에서 서버 전용 테이블은 service_role만, 사용자 관련 테이블(menus/user_profiles/user_menu_permissions)은 authenticated 전용으로 강화 시도.
- 바로 다음 `024_revert_rls_policy_changes.sql`에서 023의 변경이 전면 롤백되어 다시 전체 허용 정책으로 복귀함. 정확한 롤백 사유는 커밋 메시지에 명시되어 있지 않음(앱 코드 호환성 문제 등으로 추정).
- admin 판별은 `auth.users.raw_app_meta_data.role`을 앱 코드(`isAdminUser`)에서 확인하는 방식.
- **현재 상태: RLS는 대부분 전체 허용.** 재강화 여부는 결정되지 않은 미해결 이슈 — 임의로 정책을 변경하지 말 것.

---

## 동기화 아키텍처

### 데이터 흐름

```
SAP SQL Server (SELECT 전용)
  → lib/sap-mappers.ts (컬럼 정규화)
  → lib/sync-ops.ts (Supabase upsert, BATCH_SIZE=1000)
```

### 두 가지 연동 경로 (엔티티별 자동 선택, `lib/sap-sqlserver.ts`의 `isSapSqlServerConfigured()`)

1. **SAP SQL Server 직접 접속 (권장)** — `SAP_SQL_SERVER/DATABASE/USER/PASSWORD` 모두 설정 시. 호스트에 `\`가 있으면 Named Instance 접속(포트 미사용).
2. **URL 폴백** — 위 미설정 시 `SYNC_{CUSTOMER|ITEMLIST|SALES|INAMT|SALEETC}_URL`에서 JSON(`{ data: [...] }`)을 가져와 upsert.

### 증분 vs 전체 동기화

- `GET /api/sync/{entity}/run?full=1` — 전체 재동기화. `sales/inamt/saleetc/orders`는 `getDateMinSync()`(당월 -2개월 1일, `lib/constants.ts`) 하한 적용. `customer/itemlist`는 하한 없이 전량 조회.
- `GET /api/sync/{entity}/run` (파라미터 없음) — 증분. `sync_metadata.last_synced_at`에 3일 룩백을 더해 `since`로 사용, SAP `CreateDate/UpdateDate`를 **날짜 단위**로만 비교(타임존 무관).
- `getDateMinSync()` 하한보다 이전 과거 데이터가 필요하면 `scripts/backfill-history.ts`로 하한을 우회해 별도 백필(증분 로직 밖의 예외 경로, sales/inamt/saleetc/orders 대상).

### 두 계층의 스케줄러

1. **Next.js API 라우트** — `/api/cron/auto-sync`(매일, 전체/증분 순차 실행), `/api/cron/orders-sync`(매시간, 증분). 실제 동기화 순서를 오케스트레이션하고 결과를 `daily_auto_sync_results`/`hourly_sync_results`에 기록. `CRON_SECRET`으로 Bearer 인증.
2. **로컬 트리거 스크립트** — `scripts/run-daily-sync.ts`, `scripts/run-orders-sync.ts`는 위 API를 로컬에서 HTTP로 호출하는 얇은 트리거(로직 없음). Windows 작업 스케줄러로 실행(`DEPLOY.md` 4절).

**설계 의도:** Vercel은 방화벽 등으로 SAP SQL Server에 직접 접속하기 어려울 수 있어, 실제 정기 동기화는 **localhost에서 실행 중인 앱을 기준**으로 스케줄링한다. Vercel 배포본의 `/internal/sync` 화면은 SAP 연결 정보가 없으면 오류를 반환하는 것이 정상 동작이다 (2026-08-18 트러블슈팅으로 확인).

### read-side 병합 모듈 (동기화 파이프라인과 별개)

- `lib/sales-status-queries.ts`(A안), `lib/sales-status-grouped-queries.ts`(B안) — 화면 조회 시점에 Supabase 캐시 데이터 + SAP `DLN1` 실시간 조회를 병합해 최신성 보완.

---

## API 구조 (`app/api/`)

| 카테고리 | 경로 | 설명 |
|----------|------|------|
| Auth | `/api/auth/sync` | 클라이언트 토큰 → 서버 세션 쿠키 동기화 |
| Cron | `/api/cron/auto-sync`, `/api/cron/orders-sync` | 일괄/시간별 자동 동기화 오케스트레이션 |
| Sync 실행 | `/api/sync/{entity}/run` | SAP 직접조회 또는 URL폴백 → Supabase upsert |
| Sync 수신 | `/api/sync/{entity}` (POST) | 외부에서 가공된 데이터 upsert만 수행 |
| Sync 상태 | `/api/sync/{status,metadata,table-counts,daily-auto-results,debug}` | 동기화 현황/이력 조회 |
| Maintenance | `/api/maintenance` | 점검 공지 조회(공개)/저장(admin) |
| Permissions | `/api/permissions` | 사용자별 메뉴 권한 조회/저장/사용자 추가 (admin 전용) |
| Customers | `/api/customers`, `/api/customers/manage` | 거래처 자동완성 검색 / 관리 화면 조회·수정 |
| Dashboard | `/api/dashboard/{summary,insights,pareto,ranking,customer/[cardcode]}` | 대시보드 집계 |
| Report | `/api/report`, `/api/sales-status*` | 거래처 현황 보고서 3종 |

---

## 프론트엔드 구현 현황 (`app/internal/`)

| 화면 | 경로 | 상태 |
|------|------|------|
| 로그인 | `login/page.tsx` | 완료 |
| 인트로/랜딩 | `(protected)/page.tsx` | 완료 |
| 대시보드 | `(protected)/dashboard/` | 완료 |
| 거래처 현황 보고서 (마감기준) | `(protected)/report/` | 완료 |
| 거래처 현황 (판매기준, A안) | `(protected)/sales-status/` | 완료 |
| 거래처 현황 (판매집계, B안) | `(protected)/sales-status-b/` | 완료 |
| 동기화 관리 | `(protected)/sync/` | 완료 |
| 거래처 관리 | `(protected)/customers/` | 완료 |
| 권한 관리 (admin) | `(protected)/permissions/` | 완료 |
| 점검 모드 안내 | `maintenance/page.tsx` | 완료 |
| 미인증 안내 | `unauthorized/page.tsx` | 완료 |

인증/보호 로직: `middleware.ts`(세션 쿠키 갱신, 점검 모드 리다이렉트, OAuth 콜백 처리) + `(protected)/layout.tsx`(세션·권한 없으면 로그인/미인가 페이지로 리다이렉트, 메뉴 권한 기반 사이드바 렌더링).

---

## 배포 전략

- GitHub(`IT-KHJ/ELTSAP`) → Vercel (대시보드 또는 CLI)
- Vercel 환경변수: Supabase 관련은 필수, `SAP_SQL_*`/`SYNC_*_URL`은 선택(설정 시 Vercel에서도 직접 동기화 가능하나 방화벽 이슈 가능성 있음)
- 실제 정기 동기화는 **localhost 앱 + Windows 작업 스케줄러** 조합 사용 (`DEPLOY.md` 참조)
- Supabase Auth Redirect URL을 Vercel 배포 도메인에 맞춰 별도 등록 필요

---

## 환경변수 (`.env.local`)

| 키 | 필수여부 | 비고 |
|----|----------|------|
| `NEXT_PUBLIC_SUPABASE_URL` | 필수 | |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 필수 | |
| `SUPABASE_SERVICE_ROLE_KEY` | 필수 | 동기화/집계용 admin 클라이언트 |
| `SAP_SQL_SERVER` | 선택(권장) | `호스트` 또는 `호스트\인스턴스` |
| `SAP_SQL_DATABASE` | 선택(권장) | |
| `SAP_SQL_USER` / `SAP_SQL_PASSWORD` | 선택(권장) | |
| `SAP_SQL_PORT` | 선택 | 기본 1433, 인스턴스 사용 시 생략 |
| `SAP_SQL_TRUST_CERT` | 선택 | 기본 true |
| `SAP_SQL_ENCRYPT` | 선택 | 기본 false (원격 IP 접속 시 TLS 단계 hang 방지 목적) |
| `SAP_SQL_INSTANCE` | 선택 | 명시적 인스턴스명 |
| `SYNC_CUSTOMER_URL` / `SYNC_ITEMLIST_URL` / `SYNC_SALES_URL` / `SYNC_INAMT_URL` / `SYNC_SALEETC_URL` | 선택 | SAP 직접 연결 미설정 시 폴백 |
| `CRON_SECRET` | 선택 | cron API Bearer 인증, 16자 이상 권장 |
| `MAINTENANCE_MODE` | 선택 | true 시 전체 요청을 점검 페이지로 리다이렉트 |

> 로컬 `.env.local`에는 Supabase 3종 + `SAP_SQL_SERVER/DATABASE/USER/PASSWORD/PORT`가 설정되어 있음 (2026-08-18 확인, 값은 비공개).

---

## 변경 이력

| 시기 | 내용 |
|------|------|
| 2026-08-18 | 파일 최초 생성. git log(45개 커밋) + migrations(001~024) + app/lib/scripts 전수 조사 기반으로 전체 컨텍스트 정리 |
| 2026-03-04 | 프로젝트 최초 커밋. SAP 연동 거래처/품목/매출 동기화 및 보고서 기본 구조 |
| 2026-03-04~03-10 | 거래처 현황 보고서 고도화, 매출/반품 DB 집계, 대시보드 필터/거래처 상세 분석, 매일 자동 동기화(Cron) 도입 |
| 2026-03-16~03-17 | 거래처 현황 판매기준 A안/B안 두 가지 뷰 구현, 사용자별·메뉴별 권한 관리 시스템 신규 구현 |
| 2026-03-24~03-25 | 판매(orders) 동기화 추가, 일괄 전체 동기화 + 매시간 cron + 동기화 이력 UI, 증분 동기화 3일 룩백 적용 |
| 2026-03-30~03-31 | 데이터 정합성/UX 버그 수정 집중기. SAP 삭제 반영 로직 제거, 거래처 지역(sido/sigun) 관리·검색 추가, 메뉴 순서 sort_order 반영 |
| 2026-04-01 | 전체 동기화 기준일자를 "당월 -2개월 1일"로 변경, 보안 강화 및 권한 수정 (RLS 023→024 롤백 포함, 가장 최근 커밋) |
| 2026-08-18 | `scripts/backfill-history.ts` 신규 작성(미커밋) — 과거 데이터 백필 스크립트. 거래처/품목 전체 동기화 오류 조사(결론: 코드 버그 아님, 실행 환경별 SAP 연결 정보 유무 문제로 추정) |

> 업데이트 규칙: 새로운 결정, 파일 경로, 설계 변경이 생기면 해당 섹션을 수정하고 변경 이력에 추가한다.
