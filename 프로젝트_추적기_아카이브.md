# ELTSAP 프로젝트 추적기 — 아카이브 (1단계~6단계)

> 완료된 과거 작업 보존용입니다. 현재 진행 중 작업은 `프로젝트_추적기.md`를 참조하세요.
> 아카이브 기준: git log 전체 (main 브랜치 단일 구조, feature 브랜치 없이 순차 커밋, 2026-03-04~2026-04-01, 총 45개 커밋)

---

## 진행 규칙

- 완료 항목: `[x]`
- 미완료 항목: `[ ]`
- 부분 완료/검토 필요: `[~]`
- 결과물 파일이 있으면 경로를 괄호 안에 명시

---

## 1단계 — 초기 셋업 & 배포 안정화 (2026-03-04)

- [x] 프로젝트 최초 커밋 — SAP 연동 거래처/품목/매출 동기화 및 보고서 기본 구조 (`108c5cf`)
- [x] Vercel 빌드 오류 수정 3건 (`@types/mssql` 추가, Supabase 클라이언트 초기화 지연, `vercel.json` framework 설정)
- [x] 로그인 화면 CI 로고 적용, 거래처 검색 UX 개선(빈 칸 클릭 시 전체 목록 표시)

## 2단계 — 거래처 현황 보고서 고도화 (2026-03-04~03-10)

- [x] 거래처 현황 보고서 통합 테이블 구조, 채권/증정수량 스타일, 증감률 표시 수정
- [x] 매출/반품 DB 집계 함수 적용 (`007_add_report_sales_function.sql`, `008_add_dashboard_sales_agg.sql`, `009_add_sales_by_basecard.sql`)
- [x] 인쇄 레이아웃(여백·행 높이) 조정
- [x] 대시보드 거래처 필터/검색, 거래처 상세 분석(매출 집중도 차트) 개선
- [x] `sales` 테이블에 `linestatus`(`010_add_sales_linestatus.sql`), `vatsumsy`(`013_add_sales_vatsumsy.sql`) 컬럼 추가
- [x] 매일 자동 동기화(Cron) 및 설정 화면 결과 표시 도입 (`012_add_daily_auto_sync_results.sql`)

## 3단계 — 권한 시스템 & 다중 보고서 (2026-03-16~03-17)

- [x] 테이블명 소문자 통일 등 정리
- [x] 거래처 현황(판매기준) A안, (판매집계) B안 두 가지 보고서 뷰 구현 (`014_add_sales_status_grouped.sql`)
- [x] 사용자별·메뉴별 권한 관리 시스템 신규 구현 (`015_add_menu_permissions.sql`, `e1382a6`)
- [x] 권한 시스템 타입 오류 후속 수정 다수

## 4단계 — 동기화 기능 확장 (2026-03-24~03-25)

- [x] 판매(orders) 동기화 추가 (`016_add_orders_table.sql`), SAP 삭제 반영 로직 1차 추가
- [x] 로컬 동기화 트리거 스크립트 도입 (`scripts/run-daily-sync.ts`, `scripts/run-orders-sync.ts`)
- [x] 동기화 이력 UI(판매 포함) 추가, 매시간 cron + daily 결과 처리 (`017_add_orders_count_to_daily_auto_sync.sql`, `018_daily_and_hourly_sync_history.sql`)
- [x] 증분 동기화에 3일 룩백(lookback) 적용 (`8a84060`) — 데이터 누락 방지
- [x] 판매현황(B안)에 다중 확장 행(multi-expand rows), 엑셀 상세 블록 export 추가 (`710221c`)

## 5단계 — 데이터 정합성/UX 버그 수정 집중기 (2026-03-30~03-31)

- [x] SAP 물리적 삭제 건 반영 로직 제거(정합성 이슈 대응, `8fb809e`)
- [x] 거래처 관리 저장 시 자동 조회 기능, `customer.useyn` 조회조건 추가 (`019_add_customer_useyn.sql`, 이후 `020_drop_customer_useyn_trigger.sql`에서 보존 트리거 제거)
- [x] 속도 개선 (`46cfafa`)
- [x] 거래처 지역(sido/sigun) 관리·검색 신규 추가 (`021_add_customer_sido_sigun.sql`), 지역코드는 동기화 대상에서 제외 처리, 관련 오류 다수 수정
- [x] 메뉴 순서를 `menus.sort_order` 컬럼 기반으로 반영 및 오류 수정
- [x] TypeScript 타입 오류로 인한 Vercel 빌드 오류 수정 (`c31e3c0`)

## 6단계 — 마무리 정비 (2026-04-01)

- [x] 전체 동기화 기준일자를 "당월 -2개월 1일"로 변경 (`getDateMinSync`, `45861d5`)
- [x] 보안 강화 및 권한 수정 — RLS `023_fix_rls_always_true_policies.sql`(강화 시도) → `024_revert_rls_policy_changes.sql`(전면 롤백) 포함 (가장 최근 커밋 `454926e`)

---

## 결과물 파일 목록 (6단계까지 기준)

| 항목 | 파일 경로 | 상태 |
|------|-----------|------|
| DB 마이그레이션 (001~024) | `supabase/migrations/` | 적용 완료 |
| SAP 연동 모듈 | `lib/sap-sqlserver.ts`, `lib/sap-mappers.ts` | 완료 |
| 동기화 실행/배치 upsert | `lib/sync-ops.ts`, `lib/sync-metadata.ts` | 완료 |
| 일괄/시간별 동기화 이력 | `lib/daily-auto-sync.ts` | 완료 |
| 거래처 현황 보고서(마감기준) | `lib/report-queries.ts`, `lib/report-excel.ts`, `app/internal/(protected)/report/` | 완료 |
| 거래처 현황(판매기준 A안) | `lib/sales-status-queries.ts`, `app/internal/(protected)/sales-status/` | 완료 |
| 거래처 현황(판매집계 B안) | `lib/sales-status-grouped-queries.ts`, `lib/sales-status-grouped-excel.ts`, `app/internal/(protected)/sales-status-b/` | 완료 |
| 대시보드 | `lib/dashboard-queries.ts`, `app/internal/(protected)/dashboard/` | 완료 |
| 권한 관리 시스템 | `lib/menu-permissions.ts`, `app/api/permissions/`, `app/internal/(protected)/permissions/` | 완료 |
| 거래처 관리 화면 | `app/internal/(protected)/customers/` | 완료 |
| 로컬 스케줄러 트리거 스크립트 | `scripts/run-daily-sync.ts`, `scripts/run-orders-sync.ts` | 완료 |
| 점검 모드 | `middleware.ts`, `lib/maintenance-metadata.ts`, `app/internal/maintenance/` | 완료 |

---

## 메모

- Vercel 배포와 SAP 직접 연동은 방화벽 제약으로 분리 운영(로컬 실행 앱 + Windows 작업 스케줄러). Vercel 환경변수에는 `SAP_SQL_*`/`SYNC_*_URL`이 미설정일 수 있으며, 이는 설계상 정상 상태.
- RLS는 `023`→`024`로 강화 후 롤백된 상태로 남아 있음. 재강화 여부는 결정되지 않은 미해결 설계 이슈 (`프로젝트_내용.md` 참조).
- `sales-status`(A안)와 `sales-status-b`(B안)는 독립 구현으로 코드 중복이 있음 — 통합 여부는 논의된 바 없음.
